syntax = "proto3";

option go_package = "CoreBasedGoLang/privateKey;privatekey";  // ← اضافه کن

package privatekey;


service PrivateKeyService {
  rpc ReceivePrivateKey (PrivateKeyMessage) returns (Ack);
}

message PrivateKeyMessage {
  string private_key = 1;
}
message Ack {
  bool success = 1;
}
